/**
 * Single source of truth for public-facing growth stats.
 * Update these values as the numbers grow.
 */
export const WAITLIST_COUNT = 500;
export const BETA_COUNT = 50;

/** Display-ready strings */
export const WAITLIST_DISPLAY = `${WAITLIST_COUNT}+`;
export const BETA_DISPLAY = `${BETA_COUNT}+`;
